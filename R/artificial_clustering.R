artificial_clustering <- function(points) {
  # it will mimic hclust results, but it will put all points into the same cluster with height equal to max(abs(points))

 l_infty <- max(abs(c(0, points)))

}
